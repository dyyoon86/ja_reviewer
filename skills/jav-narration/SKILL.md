---
name: jav-narration
description: 딸딸기튜브 신작 해설영상용 — 일본어 풀 SRT + 품번을 받아 jav_2026.db에서 정보 긁어와 ① 대사 번역 SRT ② 내레이션 SRT(2트랙)를 만든다. "내레이션 만들어", "품번 ○○ 내레이션", "대사+해설 SRT", "딸딸기튜브 영상 대본" 요청 시 사용. 풀 SRT에서 선정적(신음·탄성·반복) 제외하고 스토리 대사만 추출해 전체 전개를 처리. 평가는 영상 못보니 창작.
---

# 딸딸기튜브 신작 해설 내레이션 (품번+SRT → 2트랙 SRT)

상세 배경: `jav_scrap/docs/ddalddalgi-narration.md`. 번역은 [[jav-subtitle-translate]] 스킬 사용.

## 입력
- **일본어 SRT** (작품 대사, 타임코드 포함) — 사용자가 줌
- **품번** (예: SNOS-183) — DB 조회 키

## 워크플로우

### 1) 품번으로 DB 정보 싹 긁기
`jav_scrap/gen_narration.py` 의 `fetch_meta(con, code)` 사용(또는 동일 SQL):
- works: 배우(한/일)·레이블·메이커·감독·시리즈(series_ja)·일본원제(title_ja)·**한국어 시놉시스(description)**·발매·런타임·태그·인기(views/likes/dislikes)
- actresses 조인: **스리사이즈 실수치**(bust/cup/waist/hip/height)·생일·혈액형
```bash
cd /home/dyyoon/jav_scrap && ./venv/bin/python -c "
import sqlite3,gen_narration as g
m=g.fetch_meta(sqlite3.connect('jav_2026.db'),'<품번>'); print(m); print(g.meas_str(m))"
```

### 2) 풀 SRT에서 스토리 대사만 추출 → 번역
- **풀 SRT 전체를 스캔**하되, 아래는 **제외**:
  - 신음·짧은 탄성·반복 감탄(あぁ/イク 류), 의미 없는 짧은 라인
  - 단순 섹스 장면의 비스토리 대사
- **남기는 것 = 스토리 전개에 필요한 대사**: 설정·관계·상황 전환·갈등·결말을 드러내는 대사 (도입부만이 아니라 **전체 타임라인에 걸쳐** 듬성듬성 추출).
- 추출한 스토리 대사만 [[jav-subtitle-translate]] 규칙으로 번역. Whisper 오인식은 맥락으로 복원.
- → `<원본>.대사.srt` (원본 타임코드 유지, 한국어 대사. 섹스 구간은 비어 있음).
- ※ 토큰 절약은 "초반부 자르기"가 아니라 **"선정적·반복 제거"**로 달성한다.

### 3) 내레이션 생성 (리뷰형 + 장면 해설)
DB 메타 + SRT 줄거리를 합쳐 작성. **시청자에게 말 걸듯이** — 구어체 리뷰어 톤.

- **톤**: **다큐 나레이터 말투** — 두 가지 어미만 사용.
  - ① 사실·소개 → 평서문 (`~입니다`)
  - ② 대사 직전 상황 설명 → `~는데..` / `~게 되는데..` (이 뒤에 실제 배우 대사 등장)
- **구조**: 내레이션이 장면을 예고 → 실제 대사가 이어받음. 내레이션은 대사 직전 공백에 배치.
  - 예: `'오늘 배우는 ○○입니다.'` → (대사) → `'그런데 촬영팀이 갑자기 말을 걸게 되는데..'` → (대사)
- **금지**: `~더라/~거든/~잖아/~네요` 등 리뷰어 말투, 의견·평가·호불호
- **구성**: 인트로(짧게 "다음은 ○○ 주연 ○○입니다") → 설정 설명 → 솔직 평가(피지컬·컨셉적합·완성도) → 총평. 섹스 스킵 브릿지는 "이후 ○○으로 넘어가는데요" 수준.
- **사실성**: 배우·신체·레이블·시놉시스·인기는 DB 근거. 영상 디테일(연기·완성도·비교)은 **창작**, 설정과 모순 없게.
- **별점**: 인기지표(좋아요/싫어요 비율) 참고해 ★1~5.
- 각 내레이션 문장을 **영상 타임라인 지점**에 배치(인트로 공백 + 전환점 + 코멘트 포인트).
- → `<원본>.내레이션.srt`.

**[AI 문체 금지]** 다음 패턴 절대 쓰지 말 것:
① '작품은/영상은' 주어로 시작  ② '~에 초점을 둔 구성'  ③ '~의 훅이 작동합니다'  ④ '~긴장감을 밀어붙입니다'  ⑤ 수식어 3개 이상 겹친 수식절

**[좋은 예 / 나쁜 예]**
- ❌ `이차러브 감정선에 초점을 둔 구성이네요` → ✅ `상황극 없이 이차러브로만 간다고 직접 얘기하더라고요`
- ❌ `작품의 불안한 훅이 본격적으로 작동합니다` → ✅ `여기서부터 분위기 이상해지는 거 보이시죠`
- ❌ `다우너 무드와 통제감이 흐려지는 긴장감을 밀어붙입니다` → ✅ `분위기 점점 흐려지는 전개거든요`

**[유튜브 안전 표현 필수]** 약물 직접 언급 금지:
- 금지: `약 먹이다/마시다`, `약물`, `수면제`, `최음제`, `약 기운`, `약 효과`
- 대체: `몽롱물`, `다우너 시추에이션`, `분위기가 이상해지다`, `몸 상태가 달라지다`, `몽롱하게 흘러가다`
- 예: ❌ `약 먹이고 몽롱하게 만드는 설정이에요` → ✅ `몽롱물이에요, 분위기 점점 흐려지는 전개거든요`

### 4) 출력 = B안 2트랙
- `<원본>.대사.srt` + `<원본>.내레이션.srt` **별도 2파일**.
- 편집기에서 각각 다른 자막 트랙으로 → 위치/색 따로, 동시 표시. 내레이션 구간 원음 무음.
- 만든 뒤 `cokacdir --sendfile` 로 두 파일 전송.

## 규칙/한계
- **풀 SRT 전체를 본다**(초반부만 자르지 말 것). 단 **스토리에 필요한 대사만 추출**해 처리 → 토큰 절약은 "선정적·반복 제거"로 달성.
- **선정적 대사 제외**: 신음·단순탄성·반복 감탄·비스토리 섹스 대사는 빼고, 설정·관계·전개·결말 대사만 남긴다.
- **전체 전개 커버**: 도입~중반~결말까지 스토리 흐름을 다룬다. 섹스 장면 구간은 대사 대신 내레이션으로 브릿지("이후 호텔로 자리를 옮겨…").
- **codex 토큰 revoke 상태**라 gen_narration.py 무인실행 불가 → 현재는 이 스킬로 **Claude가 직접** 번역·작성. (API 키 붙으면 자동화)

## 시그니처 (브랜드, 추후 확정)
- 채널 `딸딸기튜브`, 캐치프레이즈/별점기준은 `gen_narration.py` 상단 상수 또는 합의값. 인트로/아웃트로에 녹임.

## 산출 예 (SNOS-183 세토 칸나)
- `2.대사.srt`(카페 도입 70세그) + `2.내레이션.srt`(인트로→상황설명→피지컬코멘트 6엔트리) 로 검증 완료.
